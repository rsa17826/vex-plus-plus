{
  STR(start)STR(hub)
  STR(author)STR(rssaromeo)
  STR(description)STR(shows a bug that allows a setup that saves progress like a checkpoint but without using checkpoints)
  STR(version)INT(81)
  STR(stages){
    STR(hub){
      STR(color)INT(7)
      STR(changeSpeedOnSlopes)BOOL(false)
      STR(saveTick)BOOL(false)
    }
  }
}