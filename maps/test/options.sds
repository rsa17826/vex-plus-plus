{
  STR(start)STR(conmveyers)
  STR(author)STR(convers)
  STR(description)STR(speed but death)
  STR(version)INT(67)
  STR(stages){
    STR(conmveyers){
      STR(color)INT(2)
      STR(changeSpeedOnSlopes)BOOL(false)
      STR(saveTick)BOOL(false)
    }
  }
  STRNAME(levelVersion)INT(100)
}